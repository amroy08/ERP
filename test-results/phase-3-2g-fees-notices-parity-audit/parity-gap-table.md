# Phase 3.2G Fees + Notices Parity Gap Table

| Feature | Web Status | Mobile Status | Backend Source | Gap | Required Action |
|---|---|---|---|---|---|
| **Fee Structure / Assignment Management** | Supported (Admin CRUD on structures, student profile page for assignments). | Not Supported (Not required for parent/student). | `server/src/controllers/feeController.ts` | None | None |
| **Fee Payment Collection** | Supported (Admin collections, receipt number generator). | Not Supported (Read-only for parents/students). | `server/src/controllers/feeController.ts` | None | None |
| **Parent Fee Visibility** | Supported (Student profile page / Ledger view). | Supported (`ParentFeesScreen`). | `server/src/controllers/mobileController.ts` (`getParentFees`) | The payment calculation in `getParentFees` loops through student fees and subtracts global `totalPaid` from *each* structure's amount, causing double-deduction of payments. Web uses `FeePaymentAllocation` tables. | **Bug Fix Required**: Update `getParentFees` to use `FeePaymentAllocation` values per fee structure instead of global `totalPaid`. |
| **Student Fee Visibility** | Supported (dashboard summary / student fees page). | Partial (Only a read-only `feeSummary` on `StudentHomeScreen`). | `server/src/controllers/mobileController.ts` (`getStudentDashboard` via `FeeService.getStudentLedger`) | Student fee visibility not implemented; parent fee visibility is currently the supported mobile fee flow. | None (No dedicated fees screen is required on mobile for students). |
| **Notice Management** | Supported (Admin CRUD with priority levels and target audience options). | Not Supported (Not required for parent/student/teacher). | `server/src/controllers/moduleController.ts` | None | None |
| **Parent Notice Visibility** | Supported (Notice board list). | Supported (`ParentNoticesScreen` with tap-to-expand details). | `server/src/controllers/mobileController.ts` (`getParentNotices`) | None | None |
| **Teacher Notice Visibility** | Supported (Notice board list). | Supported (`TeacherNoticesScreen` with tap-to-expand details). | `server/src/controllers/mobileController.ts` (`getTeacherNotices`) | None | None |
| **Student Notice Visibility** | Supported (Notice board list). | Partial (Dashboard list shows title and priority, but cannot expand to view content or download PDF attachments). | `server/src/controllers/mobileController.ts` (`getStudentDashboard` notices list) | Student mobile has no dedicated Notices screen; notices appear on dashboard as title and priority only. | Document this current supported behavior clearly. |
| **Notice Target Roles** | Supported (Target options for Everyone, Teachers, Parents, Students). | Supported (Filters notices by matched role containing 'all' or the user's role). | `server/src/controllers/mobileController.ts` | None | None |
| **Notice Target Class / Section** | Not Supported. | Not Supported. | `server/prisma/schema.prisma` (`Notice` model) | Not supported in database schema or UI. | None (Class/section targeting is not a requirement of the current schema/system). |
